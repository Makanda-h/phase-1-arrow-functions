const divide = function (parameter1, parameter2) {
     return parameter1 / parameter2;
}
divide (2000,100);

const square = x => x * x;

const add = (parameter3, parameter4) => parameter3 + parameter4;